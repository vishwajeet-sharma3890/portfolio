import {styled} from "../../../globalStyles";

export const TechImagesWrapper = styled.div`
  ${props => `
    display: flex;
    flex-flow: wrap;
    justify-content: center;
    grid-gap: 32px;
  `}
`